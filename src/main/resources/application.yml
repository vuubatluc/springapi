spring:
  application:
    name: e-commerce

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/e_commerce_project?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: Minhvu147@

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

  web:
    resources:
      static-locations: classpath:/static/
      cache:
        period: 0

server:
  port: 8080
  servlet:
    context-path: /api

jwt:
  signerKey: "faed9f589c3e55f64da992108879d6e0ac38f10522f356a59ed8a353447870ae"

# Logging configuration
logging:
  level:
    com.vu.springapi: DEBUG
    org.springframework.web: DEBUG
